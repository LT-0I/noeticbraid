# private/

This directory is private and must not be committed.

It contains local account configuration, private workflows, browser profile directories, local secrets references, and host-specific settings.

The root `.gitignore` excludes this directory.
