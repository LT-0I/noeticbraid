# Step 3 Authority

Step 3 authority is the union of the following records:

```text
round1_2026-04-27_step3.md (v1 PASS parts)
∪ round1_step3_form_clarification_v2.md (v2 revisions)
∪ round1_step3_form_clarification_v3.md (v3 revisions)
```

Conflict order:

```text
v3 > v2 > v1
```

Locked concepts used by Stage 0:

- project name: NoeticBraid;
- tagline: Make AI sharper. Make the user clearer;
- 45 module list;
- 12 conceptual directory names mapped into the user-approved monorepo/private layout;
- Host Runtime + Obsidian + Web Console + IM Bridge + Remote GPT collaboration;
- Source Index is module #45;
- Memory Curator is a role/protocol, not a standalone module;
- Stage 1.1 starts from a single ChatGPT Web profile, not API-only execution.
