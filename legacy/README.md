# legacy/

Legacy assets live here as read-only references. Do not develop inside legacy directories.

The previous `helixmind_phase1/state/` directory was deleted before this Stage 0 package. New architecture must start from an empty vault and cannot assume any old DPAPI vault, audit log, or radar data exists.
