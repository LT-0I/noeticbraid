# noeticbraid-sp-B-multimodel

SP-B Multimodel Alliance Runtime????????????????????????

????? T-B01~T-B05?

- `route(task_card)`?? task card ???? `ModelRoute`?
- `run_debate(route, rounds_input)`??????/?????????????? `Debate`?
- `converge(debate)`????? `Convergence`?????/??/??? objection???????????????????
- CLI?`python -m multimodel_alliance validate-fixtures|route|run-fixture`?

## ????

```powershell
python -m pytest -q
python -m multimodel_alliance validate-fixtures
python -m multimodel_alliance route examples/task_card_medium.json --pretty
python -m multimodel_alliance run-fixture multimodel_alliance/fixtures/dual_review_prompt_cycle.json --pretty
```

Python API?

```python
from multimodel_alliance import route, run_debate, converge

model_route = route({"task_id": "task_prompt_cycle", "risk_hint": "medium"})
debate = run_debate(model_route, [{"role": "producer", "summary": "draft produced"}])
convergence = converge(debate)
```

## ??

- SP-B ???????????????????????
- SP-C2 ???? `selected_models[].invocation` ?????
- SP-H ???? `source_refs[]`?????????????????????
- ???????? `noeticbraid/tools/multimodel_alliance/`?????????

## ??

???? `jsonschema>=4,<5`???? `pytest>=8,<9`???? `setuptools>=68`?License gate ???????? `docs/DEPENDENCY_LICENSES.md`?
