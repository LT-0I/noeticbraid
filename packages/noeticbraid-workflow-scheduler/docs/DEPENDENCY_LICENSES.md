# Dependency Licenses

Runtime dependencies: none outside Python standard library.

Dev/build dependencies checked by HBA gate:

- `pytest` ? MIT.
- `setuptools` ? MIT.

No pywin32, mcp-server-sqlite, portalocker, APScheduler, Celery, Prefect, Temporal, Redis, broker, browser automation, or Telegram bot library is introduced.
