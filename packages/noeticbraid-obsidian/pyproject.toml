[project]
name = "noeticbraid-obsidian"
version = "0.1.0-stage0"
description = "Obsidian bridge interfaces, vault boundary scanner, and template surfaces."
requires-python = ">=3.11"
license = { text = "Apache-2.0" }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/noeticbraid_obsidian"]
